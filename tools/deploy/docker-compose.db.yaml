version: "3.2"
services:
  db:
    image: "postgres"
    container_name: "db"
    restart: unless-stopped
    environment:
      - POSTGRES_USER=${DB_USER_NAME}
      - POSTGRES_PASSWORD=${DB_PASS}
      - POSTGRES_DB=${DB_NAME}
    ports:
      - "5433:5432"
    volumes:
      - sht-db-data:/var/lib/postgresql/data
    networks:
      - database
  db-update:
    command: update
    build:
      context: ./../../src
      dockerfile: ./../tools/deploy/db.migrations.Dockerfile
    environment:
      - ConnectionOptions__ConnectionString=${ConnectionOptions__ConnectionString}
    networks:
      - database
networks:
  database:
volumes:
  sht-db-data: